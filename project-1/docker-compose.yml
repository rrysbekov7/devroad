version: '3.8'

services:
  portfolio:
    build: .
    container_name: portfolio
    restart: always
    ports:
      - "8080:80"
    # volumes:
    #   - .:/usr/share/nginx/html   # раскомментировать для разработки
