name: Environment tasks

on: push
env:
  GLOBAL_VAR: I am a global env
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Install Checkout
        uses: actions/checkout@v4
      - name: Print Job new env
        env:
          MY_NEW_ENV: Books
        run: echo "I like to read $MY_NEW_ENV"