name: 5 Steps task

on: push

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Install Checkout
        uses: actions/checkout@v4
      - name: Print Hello step 1
        run: echo "Hello world!"
      - name: Print PWD step 2
        run: pwd 
      - name: Print ls step 3
        run: ls -la
      - name: Print date step 4
        run: date 
      -  run: echo "echo-1"
      -  run: echo "echo-2"
      -  run: echo "echo-3"
      #without name step 5
      - run: tasklist 
